
# 
# Tmux config v2
#

# Plugins

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'dracula/tmux'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# Options

set -g mouse on
set -g status-position top   # Status bar on Top
set -g base-index 1          # Windows start at 1 instead of 0
set -g renumber-windows on   # Keep window numbers correct
set -g history-limit 1000000
set -g detach-on-destroy off # Don't exit temux when closing a session
setw -g mode-keys vi

# Hotkeys

# Set Prefix
unbind C-b
set -g prefix C-Space

# Split Window
bind d split-window -h
bind k split-window -v

# Run tpm
run '~/.config/tmux/plugins/tpm/tpm'
